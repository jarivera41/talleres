class maraton{
     public static void main (String[] arguments){
     
     System.out.println("cuantas personas corrieron en la maraton ");
     String antidad = System.console().readLine();
     int c;
        c = Integer.parceInteger(c);
     int[] andidatos= new[c];
     int[] iempo= new[c];  

      for (i=0 ; i<candidatos.length ; i++){
      System.out.println("ingrese el nombre del corredor");
      String a= System.console().readLine();
      int b= integer.parseInt(a);
      candidatos[i]=b;
      System.out.println("ingrese el tiempo del corredor ");
      a= System.console().readLine();
      b= Integer.parseInt(a);
      tiempo[i]=b;
    }      
      int aux;
      String aux1;
      for(i=0 ; i< candidatos.length ; i++){
      
       for(j=1 ; j< candidatos.length ; i++){
                  
              if( iempo[i]< tiempo[j]){
                     aux= tiempo[i];
                     tiempo[i]=tiempo[j];
                     tiempo[j]= aux;
                     
                     aux1= andidato[i];
                     andidato[i]=andidato[j];
                     andidato[j]= aux;

                       }
          }    
    

    
    }
    
     System.out.println("el primer puesto es :" andidato[0] " con un tiempo de " tiempo [0] );
     System.out.println("el segundo puesto es :" andidato[1] "con un tiempo de " tiempo[1] );

   }
}

